# Задачи на рекурсию

## Уровень 1: Базовые задачи

### Задача 1.1: Факториал
Напиши функцию, которая вычисляет факториал числа n.

**Формула:** n! = n × (n-1)!

**Базовый случай:** 0! = 1

**Примеры:**
```
factorial(5) → 120
factorial(0) → 1
factorial(3) → 6
```

**Подсказка:** Подумай, как факториал 5 зависит от факториала 4.

---

### Задача 1.2: Степень
Напиши функцию, которая вычисляет a в степени b (a^b) рекурсивно.

**Формула:** a^b = a × a^(b-1)

**Базовый случай:** a^0 = 1

**Примеры:**
```
power(2, 5) → 32
power(3, 3) → 27
power(5, 0) → 1
```

---

### Задача 1.3: Сумма цифр
Напиши функцию, которая считает сумму всех цифр числа.

**Примеры:**
```
sum_of_digits(123) → 6 (1 + 2 + 3)
sum_of_digits(999) → 27 (9 + 9 + 9)
sum_of_digits(0) → 0
```

**Подсказка:** Как выделить последнюю цифру? (используй модуль %)

---

## Уровень 2: Работа со строками и последовательностями

### Задача 2.1: Палиндром
Напиши функцию, которая проверяет, является ли строка палиндромом (читается одинаково в обе стороны).

**Примеры:**
```
is_palindrome("racecar") → True
is_palindrome("hello") → False
is_palindrome("a") → True
is_palindrome("") → True
```

**Подсказка:** Сравни первый и последний символ. Если они совпадают, проверь остаток строки.

---

### Задача 2.2: Перевёртывание строки
Напиши функцию, которая переворачивает строку рекурсивно.

**Примеры:**
```
reverse_string("hello") → "olleh"
reverse_string("abc") → "cba"
reverse_string("a") → "a"
reverse_string("") → ""
```

---

### Задача 2.3: Бинарный поиск в отсортированном списке
Напиши функцию, которая ищет элемент в отсортированном списке рекурсивно.

**Примеры:**
```
binary_search([1, 3, 5, 7, 9, 11], 7) → True
binary_search([1, 3, 5, 7, 9, 11], 6) → False
binary_search([2, 4, 6], 2) → True
```

**Подсказка:** Раздели список пополам. Если элемент в середине больше, ищи в левой половине. Если меньше — в правой.

---

## Уровень 3: Классические рекурсивные алгоритмы

### Задача 3.1: Числа Фибоначчи
Напиши функцию, которая вычисляет n-е число Фибоначчи.

**Формула:** F(n) = F(n-1) + F(n-2)

**Базовые случаи:** F(0) = 0, F(1) = 1

**Примеры:**
```
fibonacci(6) → 8 (0, 1, 1, 2, 3, 5, 8)
fibonacci(0) → 0
fibonacci(1) → 1
fibonacci(5) → 5
```

**Важно:** Эта функция медленная для больших чисел! Попробуй с числами до 35, чтобы понять, почему.

---

### Задача 3.2: Сумма элементов списка
Напиши функцию, которая считает сумму всех элементов в списке рекурсивно.

**Примеры:**
```
sum_list([1, 2, 3, 4, 5]) → 15
sum_list([10]) → 10
sum_list([]) → 0
```

**Подсказка:** Сумма списка = первый элемент + сумма оставшихся элементов.

---

### Задача 3.3: Поиск максимума в списке
Напиши функцию, которая находит максимальный элемент в списке рекурсивно.

**Примеры:**
```
find_max([3, 1, 4, 1, 5, 9, 2, 6]) → 9
find_max([5]) → 5
find_max([-10, -3, -1]) → -1
```

---

## Уровень 4: Сложнее

### Задача 4.1: Генерирование всех подмножеств (Power Set)
Напиши функцию, которая возвращает все возможные подмножества списка.

**Примеры:**
```
power_set([1, 2]) → [[], [1], [2], [1, 2]]
power_set([1]) → [[], [1]]
power_set([]) → [[]]
```

**Подсказка:** Для списка [1, 2, 3] думай так:
- Все подмножества без 3
- Все подмножества без 3, но с добавленной 3 к каждому

---

### Задача 4.2: Подсчёт количества способов подняться на лестницу
На лестницу с n ступенями можно подняться, преодолевая за раз 1 или 2 ступени. Сколько разных способов?

**Примеры:**
```
climb_stairs(1) → 1 (только 1+1)
climb_stairs(2) → 2 (1+1 или 2)
climb_stairs(3) → 3 (1+1+1 или 1+2 или 2+1)
climb_stairs(4) → 5
```

---

### Задача 4.3: Сумма чисел в вложенном списке
Напиши функцию, которая считает сумму всех чисел в списке, который может содержать вложенные списки.

**Примеры:**
```
sum_nested([1, [2, 3], [4, [5, 6]]]) → 21
sum_nested([1, 2, 3]) → 6
sum_nested([]) → 0
```

**Подсказка:** Проверяй, является ли элемент числом или списком. Если список — рекурсивно суммируй его содержимое.

---

## Дополнительный вызов

### Задача 5: Обход файловой системы
Напиши функцию, которая **выводит все файлы** в папке и её подпапках.

**Формат:**
```python
def list_files(path, indent=0):
    """
    path - путь к папке
    indent - уровень отступа (для красоты вывода)
    """
    # твой код здесь
```

**Подсказка:** Используй модуль `os`. Для каждой папки вызови функцию рекурсивно.

```python
import os

for item in os.listdir(path):
    full_path = os.path.join(path, item)
    if os.path.isdir(full_path):
        # это папка — рекурсия!
    else:
        # это файл
```

---

## Рекомендуемый порядок решения

1. **Сегодня:** 1.1, 1.2, 1.3 (привыкни к синтаксису)
2. **Завтра:** 2.1, 2.2 (поработай со строками)
3. **Когда почувствуешь уверенность:** 3.1, 3.2, 3.3 (классика)
4. **Потом:** 4.1, 4.2, 4.3 (вызов)
5. **Напоследок:** задача 5 (реальный пример)

---

## Советы при решении

✅ **Сначала найди базовый случай** — когда рекурсия должна остановиться

✅ **Потом определи рекурсивный случай** — как проблема разбивается на меньшую версию

✅ **Проверяй логику на бумаге** перед кодированием

✅ **Тестируй с малыми значениями** — легче понять, что происходит

✅ **Используй `print()`** для отладки, чтобы видеть, как функция "разворачивается"

---

**Удачи! 💪**
